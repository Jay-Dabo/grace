<%= simple_form_for([@church, @group, @group_member]) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :member_id, collection: @members, label_method: "full_name", label_value: "member_id", input_html: { class: "form-control" } %>
  </div>

  <div class="form-actions">
    <%= f.button :submit, "Add Member to Group", class: "btn main-btn block" %>
  </div>
<% end %>
